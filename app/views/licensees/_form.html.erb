<%= form_for(@licensee) do |f| %>
  <% if @licensee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@licensee.errors.count, "error") %> prohibited this licensee from being saved:</h2>

      <ul>
      <% @licensee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
